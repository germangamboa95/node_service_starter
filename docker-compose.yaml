version: "3.9"
services:
  api:
    build: ./
    volumes:
      - ./:/usr/app/src
    command: npm run start:dev
  db:
    image: postgres
    restart: always
    ports:
      - 9999:5432
    environment:
      POSTGRES_PASSWORD: example
